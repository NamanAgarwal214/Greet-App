extends baseEmail

block content
  p Hi #{firstName},
    h2 Today is your friend #{subject.friend.name}'s #{subject.friend.event}.
    h4 Be first one to wish them and make them feel special!
    table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
      tbody
        tr
          td(align='left')
            table(role='presentation', border='0', cellpadding='0', cellspacing='0')
              tbody
                tr
                  td
                    //- a(href=`${url}`, target='_blank') Reset your password
    p If you don't have a friend by that name, please ignore this email!`
